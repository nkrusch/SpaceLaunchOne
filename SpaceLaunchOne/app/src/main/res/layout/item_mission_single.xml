<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/mission_content">

    <TextView
        android:id="@+id/mission_icon"
        style="@style/mission_icon" />

    <TextView
        android:id="@+id/mission_no"
        style="@style/mission_no" />

    <TextView
        android:id="@+id/mission_description"
        style="@style/mission_description" />

    <Button
        android:id="@+id/mission_expand_text_button"
        style="@style/mission_toggle_button" />


</android.support.constraint.ConstraintLayout>
